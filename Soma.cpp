//Leia 2 variáveis, denominadas A e B e faça a soma dessas duas variáveis, atribuindo seu resultado à variável X .

#include <stdio.h>

int main (){
	int a,b, x; //sensitive case
	printf("Usuario! Digite o primeiro número: ");
	scanf("%d", &a);
	printf("Usuario! Digite o segundo número: ");
	scanf("%d", &b);
	x = a + b; //comando de atribuição = ; operador matemático +
	printf("X = %d", X);
	  
	printf(a);//para o computador é uma váriavel
	printf("a");//virou um texto
	
	printf("X = %d\n", x);
	printf("X = %d\n", &x);
	printf("X = %x\n", &x)
}
